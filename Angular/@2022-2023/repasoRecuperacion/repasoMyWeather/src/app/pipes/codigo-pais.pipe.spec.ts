import { CodigoPaisPipe } from './codigo-pais.pipe';

describe('CodigoPaisPipe', () => {
  it('create an instance', () => {
    const pipe = new CodigoPaisPipe();
    expect(pipe).toBeTruthy();
  });
});
